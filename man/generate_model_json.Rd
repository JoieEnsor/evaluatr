% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_model_json.R
\name{generate_model_json}
\alias{generate_model_json}
\title{Generate an obfuscated JSON model specification file}
\usage{
generate_model_json(
  model = NULL,
  coefficients = NULL,
  model_type = NULL,
  model_id = NULL,
  model_name = NULL,
  outcome_type = NULL,
  variables = NULL,
  description = "",
  version = "1.0",
  preprocessing = NULL,
  model_parameters = NULL,
  reference_category = NULL,
  output_dir = ".",
  output_filename = NULL
)
}
\arguments{
\item{model}{A fitted model object. Supported classes: \code{glm} (binomial
family), \code{coxph}, \code{survreg} (Weibull/exponential), \code{multinom} (nnet).
Exactly one of \code{model} or \code{coefficients} must be non-NULL.}

\item{coefficients}{A named numeric vector (for flat models) or named list
of named numeric vectors (for multinomial, one entry per non-reference
category). Exactly one of \code{model} or \code{coefficients} must be non-NULL.}

\item{model_type}{Character. Required when \code{coefficients} is supplied.
One of \code{"logistic"}, \code{"cox"}, \code{"weibull"}, \code{"multinomial"}.}

\item{model_id}{Character. Unique identifier for this model (required).
Must match the folder name in the GitHub repository (e.g. \code{"aki_v1"}).}

\item{model_name}{Character. Human-readable name (required).}

\item{outcome_type}{Character. One of \code{"binary"}, \code{"survival"},
\code{"multinomial"}. Required when \code{coefficients} is supplied; inferred
automatically from model objects.}

\item{variables}{Character vector of predictor variable names (not including
\code{"(Intercept)"}). Required when \code{coefficients} is supplied; inferred from
model objects.}

\item{description}{Character. Free-text description of the model. Default \code{""}.}

\item{version}{Character. Version string. Default \code{"1.0"}.}

\item{preprocessing}{Character or NULL. R code string that will be evaluated
on the evaluator's \code{validation_data} before prediction (e.g. to create
dummy variables). Default \code{NULL}.}

\item{model_parameters}{Named list or NULL. Additional model parameters
required for Cox and Weibull models:
\itemize{
\item Cox: \code{list(timepoints = c(...), baseline_survival = c(...))}
\item Weibull: \verb{list(timepoints = c(...), shape = <value>, parameterisation = "aft")} (shape is extracted automatically from
\code{survreg} objects but can be overridden here).
}}

\item{reference_category}{Character or NULL. For multinomial models: name of
the reference category (the category whose coefficients are all zero).
Required when \code{model_type = "multinomial"} and \code{coefficients} is supplied.
Inferred automatically from \code{multinom} objects.}

\item{output_dir}{Character. Directory to write the JSON file. Created if it
does not exist. Default \code{"."}.}

\item{output_filename}{Character or NULL. Filename for the JSON output.
Default \code{"coefficients.json"}.}
}
\value{
The complete JSON structure as an R list (invisibly). The primary
side effect is writing the JSON file to \code{{output_dir}/{output_filename}}.
}
\description{
Developer-facing utility that accepts a fitted R model object or a manual
coefficient specification and produces a correctly formatted, obfuscated
JSON file ready for upload to a private GitHub repository for use with
\code{\link[=secure_model_validation]{secure_model_validation()}}.

Coefficients are obfuscated using the Phase 1 C++ engine so that the values
stored in the JSON file are meaningless without the obfuscation key and the
compiled binary salt. The obfuscation key is embedded in the JSON file.
}
\details{
The generated JSON follows the v1 format used by the C++ prediction engine:

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "model_type": "logistic",
  "obfuscation_key": "<32-char hex>",
  "coefficients": \{ "(Intercept)": <obfuscated>, "age": <obfuscated> \},
  "preprocessing": null,
  "model_parameters": null,
  "metadata": \{
    "model_id": "...", "model_name": "...", "version": "1.0",
    "outcome_type": "binary", "variables": ["age"], "description": "..."
  \}
\}
}\if{html}{\out{</div>}}

Coefficient values are obfuscated using an affine transform seeded from the
obfuscation key and a compiled binary salt. The key is stored in the JSON
so the C++ engine can de-obfuscate at prediction time; it is NOT a secret,
but the compiled salt is. Coefficients cannot be recovered without the
compiled binary.
}
\examples{
\dontrun{
# Logistic regression from a fitted glm
model <- glm(AKI ~ creatinine_mean + bun_mean,
             data = mydata, family = binomial)
generate_model_json(
  model      = model,
  model_id   = "aki_v1",
  model_name = "AKI Prediction Model",
  output_dir = "models/aki_v1"
)

# Logistic from manual coefficients
generate_model_json(
  coefficients = c("(Intercept)" = -1.25, age = 0.02, score = 0.8),
  model_type   = "logistic",
  model_id     = "manual_v1",
  model_name   = "Manual Model",
  outcome_type = "binary",
  variables    = c("age", "score"),
  output_dir   = "models/manual_v1"
)

# Multinomial with preprocessing
model <- nnet::multinom(outcome ~ age + sex, data = mydata)
generate_model_json(
  model              = model,
  model_id           = "outcome_v1",
  model_name         = "Outcome Model",
  reference_category = "discharged",
  preprocessing      = "validation_data$sexfemale <- ifelse(validation_data$sex == 'female', 1, 0)",
  output_dir         = "models/outcome_v1"
)
}

}
