% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_model_json.R
\name{register_model}
\alias{register_model}
\title{Register a model and generate its protected specification file}
\usage{
register_model(
  model = NULL,
  coefficients = NULL,
  model_type = NULL,
  model_id = NULL,
  developer_id = NULL,
  model_name = NULL,
  outcome_type = NULL,
  variables = NULL,
  description = "",
  version = "1.0",
  preprocessing = NULL,
  model_parameters = NULL,
  reference_category = NULL,
  registrant_relationship = NULL,
  developer_name = NULL,
  developer_email = NULL,
  public_listing = TRUE,
  rate_limit_exempt = FALSE,
  output_dir = "."
)
}
\arguments{
\item{model}{A fitted model object. Supported classes: \code{glm} (binomial
family), \code{coxph}, \code{survreg} (Weibull/exponential), \code{multinom} (nnet).
Exactly one of \code{model} or \code{coefficients} must be non-NULL.}

\item{coefficients}{A named numeric vector (for flat models) or named list
of named numeric vectors (for multinomial, one entry per non-reference
category). Exactly one of \code{model} or \code{coefficients} must be non-NULL.}

\item{model_type}{Character. Required when \code{coefficients} is supplied.
One of \code{"logistic"}, \code{"cox"}, \code{"weibull"}, \code{"multinomial"}.}

\item{model_id}{Character. Unique identifier for this model (required).
Must match the folder name in the GitHub repository (e.g. \code{"dvt_v1"}).
Choose a name that is meaningful and unlikely to conflict with other
developers' models.}

\item{developer_id}{Character. Your identifier (e.g. GitHub username).
Used to attribute the model in the evaluatr registry (required).}

\item{model_name}{Character. Human-readable name (required).}

\item{outcome_type}{Character. One of \code{"binary"}, \code{"survival"},
\code{"multinomial"}. Required when \code{coefficients} is supplied; inferred
automatically from model objects.}

\item{variables}{Character vector of predictor variable names (not including
\code{"(Intercept)"}). Required when \code{coefficients} is supplied; inferred from
model objects.}

\item{description}{Character. Free-text description of the model. Default
\code{""}. Recommended: include the outcome definition, predictor list, and
the development dataset used.}

\item{version}{Character. Version string. Default \code{"1.0"}.}

\item{preprocessing}{Character or \code{NULL}. R code string that will be
evaluated on the evaluator's \code{validation_data} before prediction (e.g.
to create derived variables or dummy columns). Default \code{NULL}. The code
must modify \code{validation_data} in place; it has access to that object and
nothing else.}

\item{model_parameters}{Named list or \code{NULL}. Additional parameters
required for Cox and Weibull models:
\itemize{
\item Cox: \code{list(timepoints = c(...), baseline_survival = c(...))}
\item Weibull: \verb{list(timepoints = c(...), shape = <value>, parameterisation = "aft")}. The shape parameter is extracted
automatically from \code{survreg} objects but can be overridden here.
}}

\item{reference_category}{Character or \code{NULL}. For multinomial models: name
of the reference category (the category whose coefficients are all zero).
Required when \code{model_type = "multinomial"} and \code{coefficients} is supplied.
Inferred automatically from \code{multinom} objects.}

\item{registrant_relationship}{Character. Required. Your relationship to the
model being registered. Must be one of \code{"original_developer"} (you created
the model), \code{"authorised_proxy"} (you are registering on behalf of the
original developer with their permission), or \code{"independent"} (you are
registering a model developed by others). This is stored in the evaluatr
registry and used for endorsement decisions.}

\item{developer_name}{Character or \code{NULL}. Your name (e.g. \code{"Jane Smith"}).
Stored in the evaluatr registry so evaluators can identify the model
maintainer. Optional but recommended.}

\item{developer_email}{Character or \code{NULL}. Contact email for evaluators
who wish to request a validation token. Optional but strongly recommended
if you want your model to be discoverable via \code{\link[=list_registered_models]{list_registered_models()}}.}

\item{public_listing}{Logical. Whether to list this model in the public
evaluatr registry (returned by \code{\link[=list_registered_models]{list_registered_models()}}). Default
\code{TRUE}. Set to \code{FALSE} to keep the registration private while a paper
is under review, for example.}

\item{rate_limit_exempt}{Logical. Whether this model is exempt from the
key-fetch rate limit. Default \code{FALSE}. Set to \code{TRUE} only for demo/
teaching models intended for open repeated use.
Also useful for fully open access models e.g., specification already published open access.}

\item{output_dir}{Character. Directory to write the specification file.
Created if it does not exist. Default \code{"."}. The file will be written as
\verb{\{model_id\}_specification.json} inside this directory.}
}
\value{
The complete JSON structure as an R list (invisibly). The primary
side effect is writing the JSON file to \code{file.path(output_dir, output_filename)}.
}
\description{
Developer-facing utility that accepts a fitted R model object or a manual
coefficient specification and produces a correctly formatted, protected JSON
file ready for upload to a GitHub repository for use with
\code{\link[=secure_model_validation]{secure_model_validation()}}.

The function also registers the model with the evaluatr registry, which
is required before any evaluator can validate the model. Registration
stores the keys needed to protect and later recover coefficient values;
those keys are never written to the JSON file itself.
}
\details{
The generated file contains protected coefficient values and model
metadata. At validation time, \code{\link[=secure_model_validation]{secure_model_validation()}} reads the file
and computes predictions internally; the raw coefficient values are never
exposed as R objects in the evaluator's session.

\strong{After running this function}, upload the specification file to your
private GitHub repository at the path
\verb{<model_id>/<model_id>_specification.json}, then issue
a time-limited, fine-grained read-only access token to each evaluator. See
the developer guide vignette (\code{vignette("developer-guide", package =
"evaluatr")}) for a complete walkthrough.

This function requires a network connection to the evaluatr registry. It
will fail if the registry is unreachable.
}
\examples{
\dontrun{
# ---- Example 1: Logistic regression (DVT diagnosis model) ------------------
dvt_path <- system.file("extdata", "dvt_example.csv", package = "evaluatr")
dvt_data <- read.csv(dvt_path)

# Fit on development studies 1-5
dvt_dev <- dvt_data[dvt_data$study \%in\% 1:5, ]
dvt_fit <- glm(dvt ~ male + ab_ddimer + calf_diff + vein_distension +
                     malignancy + immobile,
               data = dvt_dev, family = binomial)

register_model(
  model          = dvt_fit,
  model_id       = "dvt_v1",
  developer_id   = "your-github-username",
  model_name     = "DVT Diagnosis Model v1",
  description    = "Logistic regression for DVT diagnosis, developed in studies 1-5.",
  developer_name  = "Your Name",
  developer_email = "you@institution.ac.uk",
  output_dir     = "dvt_v1"
)
# Upload dvt_v1/dvt_v1_specification.json to your private GitHub repository.

# ---- Example 2: Cox survival model (breast cancer relapse) ----------------
bc_path <- system.file("extdata", "bc_survival_example.csv",
                       package = "evaluatr")
bc_data <- read.csv(bc_path)
bc_dev  <- bc_data[bc_data$val == 0, ]

library(survival)
cox_fit <- coxph(Surv(rf, rfi) ~ age + nodes + pr + er + hormon,
                 data = bc_dev, x = TRUE)

# Baseline survival at 5 and 10 years (months: 60 and 120)
bh <- basehaz(cox_fit, centered = FALSE)
S0_60  <- exp(-bh$hazard[which.min(abs(bh$time - 60))])
S0_120 <- exp(-bh$hazard[which.min(abs(bh$time - 120))])

register_model(
  model            = cox_fit,
  model_id         = "bc_cox_v1",
  developer_id     = "your-github-username",
  model_name       = "Breast Cancer Relapse-Free Survival Model",
  description      = "Cox PH model for relapse-free survival, development set.",
  model_parameters = list(
    timepoints         = c(60, 120),
    baseline_survival  = c(S0_60, S0_120)
  ),
  developer_name  = "Your Name",
  developer_email = "you@institution.ac.uk",
  output_dir      = "bc_cox_v1"
)

# ---- Example 3: Manual coefficients (logistic) ----------------------------
register_model(
  coefficients = c("(Intercept)" = -3.2, ab_ddimer = 1.8,
                   calf_diff = 1.1, vein_distension = 0.9),
  model_type   = "logistic",
  model_id     = "dvt_manual_v1",
  developer_id = "your-github-username",
  model_name   = "DVT Manual Coefficients",
  outcome_type = "binary",
  variables    = c("ab_ddimer", "calf_diff", "vein_distension"),
  output_dir   = "dvt_manual_v1"
)
}

}
