% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval_performance_binary.R
\name{eval_performance}
\alias{eval_performance}
\title{Calculate Performance Metrics from a Validation Result}
\usage{
eval_performance(
  validation_result,
  generate_plots = TRUE,
  confidence_intervals = TRUE,
  n_boot = 200,
  plot_title = NULL,
  decision_threshold = 0.1,
  by = NULL,
  cal_args = list(),
  dca_args = list()
)
}
\arguments{
\item{validation_result}{List. Output from \code{\link[=secure_model_validation]{secure_model_validation()}}.}

\item{generate_plots}{Logical. Whether to produce calibration, decision
curve, and probability distribution plots. Default \code{TRUE}.}

\item{confidence_intervals}{Logical. Whether to compute 95\% bootstrap
confidence intervals. Default \code{TRUE}.}

\item{n_boot}{Integer. Number of bootstrap resamples. Default \code{200}.
Increase to 1000 for publication-quality confidence intervals.}

\item{plot_title}{Character or \code{NULL}. Optional title suffix added to
plot headings.}

\item{decision_threshold}{Numeric. Probability threshold used for net
benefit calculations in the decision curve. Default \code{0.1}. Set to the
clinically relevant threshold for your outcome.}

\item{by}{Vector or \code{NULL}. Optional subgroup vector of the same length
as the predictions (pass \code{result$shuffled_by} from the output of
\code{\link[=secure_model_validation]{secure_model_validation()}} with the \code{by} argument). When provided,
metrics are computed separately for each level of this variable.}

\item{cal_args}{Named list of additional arguments controlling the
calibration plot. Supported options:
\describe{
\item{\code{g}}{Integer. Number of groups. Default \code{10}.}
\item{\code{smooth}}{Character. Flexible calibration curve method:
\code{"loess"} (default) or \code{"rcs"}.}
\item{\code{CL.smooth}}{Whether to show the confidence band around the
flexible curve: \code{"fill"} (default shaded band), \code{"line"}, or
\code{FALSE} to suppress.}
\item{\code{xlim}}{Numeric vector of length 2. X-axis limits.
Default \code{c(0, 1)}.}
\item{\code{ylim}}{Numeric vector of length 2. Y-axis limits.
Default \code{c(0, 1)}.}
}}

\item{dca_args}{Named list of additional arguments controlling the
decision curve analysis. Supported options:
\describe{
\item{\code{thresholds}}{Numeric vector. Threshold probabilities at which
to evaluate net benefit. Default \code{seq(0, 1, by = 0.01)}.}
\item{\code{standardize}}{Logical. If \code{TRUE}, plot standardised net benefit
instead of net benefit. Default \code{FALSE}.}
\item{\code{policy}}{Character. \code{"opt-in"} (default, treat high-risk
patients) or \code{"opt-out"} (withhold treatment from low-risk patients).}
}}
}
\value{
A named list:
\describe{
\item{\code{metrics}}{Data frame of performance metrics. When \code{by} is
supplied a \code{Subgroup} column is prepended.}
\item{\code{plots}}{List of plot objects: \code{calibration}, \code{decision_curve},
and \code{distribution}. When \code{by} is supplied, plots are organised by
subgroup level: \code{plots[["level_name"]]$calibration}, etc.}
\item{\code{model_info}}{Metadata from \code{validation_result}.}
\item{\code{decision_threshold}}{The threshold value used.}
\item{\code{subgroup_variable}}{Name of the \code{by} variable (subgroup
analysis only).}
\item{\code{subgroup_categories}}{Character vector of category names
(subgroup analysis only).}
}
}
\description{
Computes a comprehensive set of discrimination, calibration, and utility
performance metrics from the shuffled prediction-outcome matrix returned
by \code{\link[=secure_model_validation]{secure_model_validation()}}. Optionally generates diagnostic plots and
bootstrap confidence intervals.
}
\details{
Metrics computed include: AUROC/c-statistic, calibration intercept,
calibration slope, ECI, ICI, ECE, O:E ratio, Brier score, Nagelkerke
R-squared, Cox-Snell R-squared, McFadden R-squared, net benefit, and
standardised net benefit.

This function currently supports \strong{binary outcomes} (0/1) only.
Multinomial support is planned for a future release.
}
\examples{
\dontrun{
# ---- Whole-dataset evaluation ----------------------------------------------
dvt_path <- system.file("extdata", "dvt_example.csv", package = "evaluatr")
dvt_val  <- read.csv(dvt_path)
dvt_val  <- dvt_val[dvt_val$study \%in\% c(6, 7), ]

result <- secure_model_validation(
  repo_owner      = "JoieEnsor",
  repo_name       = "evaluatr-demo-models",
  model_id        = "dvt_logistic_v1",
  github_token    = "evaluatr-demo",
  validation_data = dvt_val,
  outcome         = "dvt"
)

perf <- eval_performance(
  result,
  n_boot             = 200,
  decision_threshold = 0.15
)
perf$metrics

# Access individual diagnostic plots
perf$plots$calibration    # calibration plot
perf$plots$decision_curve # decision curve analysis
perf$plots$distribution   # predicted probability distribution

# ---- Subgroup evaluation by study -----------------------------------------
result_by_study <- secure_model_validation(
  repo_owner      = "JoieEnsor",
  repo_name       = "evaluatr-demo-models",
  model_id        = "dvt_logistic_v1",
  github_token    = "evaluatr-demo",
  validation_data = dvt_val,
  outcome         = "dvt",
  by              = "study"
)

perf_by_study <- eval_performance(
  result_by_study,
  by                 = result_by_study$shuffled_by,
  n_boot             = 200,
  decision_threshold = 0.15
)

# Metrics table has a Subgroup column
perf_by_study$metrics

# Access study-specific plots
perf_by_study$plots[["6"]]$calibration
perf_by_study$plots[["7"]]$calibration

# ---- Customising the calibration plot -------------------------------------
perf_custom <- eval_performance(
  result,
  n_boot             = 200,
  decision_threshold = 0.15,
  cal_args = list(smooth = "rcs", g = 5),
  dca_args = list(standardize = TRUE)
)
}

}
\seealso{
\code{\link[=secure_model_validation]{secure_model_validation()}}, \code{\link[=list_registered_models]{list_registered_models()}}
}
