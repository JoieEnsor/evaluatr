% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval_performance.R
\name{eval_performance}
\alias{eval_performance}
\title{Calculate Performance Metrics from a Validation Result}
\usage{
eval_performance(
  validation_result,
  generate_plots = TRUE,
  confidence_intervals = TRUE,
  n_boot = 200,
  plot_title = NULL,
  decision_threshold = 0.1,
  by = NULL
)
}
\arguments{
\item{validation_result}{List. Output from \code{\link{secure_model_validation}}.}

\item{generate_plots}{Logical. Whether to produce calibration, decision
curve, and probability distribution plots. Default \code{TRUE}.}

\item{confidence_intervals}{Logical. Whether to compute 95\% bootstrap
confidence intervals. Default \code{TRUE}.}

\item{n_boot}{Integer. Number of bootstrap resamples. Default \code{1000}.}

\item{plot_title}{Character or \code{NULL}. Optional title suffix for plots.}

\item{decision_threshold}{Numeric. Probability threshold used for net
benefit calculations. Default \code{0.1}.}

\item{by}{Vector or \code{NULL}. Optional subgroup vector of the same length
as the predictions (e.g. \code{result$shuffled_by}). When provided, metrics
are computed separately for each level of this variable.}
}
\value{
A named list:
\describe{
\item{\code{metrics}}{Data frame of performance metrics. When \code{by} is
supplied a \code{Subgroup} column is prepended.}
\item{\code{plots}}{List containing \code{calibration} (base-R plot, drawn to
graphics device), \code{decision_curve} (base-R plot, drawn to graphics device),
and \code{distribution} (ggplot2 object, can be stored and recalled).}
\item{\code{model_info}}{Metadata from \code{validation_result}.}
\item{\code{decision_threshold}}{The threshold value used.}
\item{\code{subgroup_variable}}{Name of the \code{by} variable (subgroup
analysis only).}
\item{\code{subgroup_categories}}{Character vector of category names
(subgroup analysis only).}
}
}
\description{
Computes a comprehensive set of discrimination, calibration, and utility
performance metrics from the shuffled prediction-outcome matrix returned
by \code{\link{secure_model_validation}}. Optionally generates diagnostic plots and
bootstrap confidence intervals.
}
\details{
Metrics computed include: AUROC/c-statistic, calibration intercept,
calibration slope, ECI, ICI, ECE, O:E ratio, Brier score, Nagelkerke R\ifelse{html}{\out{<sup>2</sup>}}{\eqn{^2}},
Cox-Snell R\ifelse{html}{\out{<sup>2</sup>}}{\eqn{^2}}, McFadden R\ifelse{html}{\out{<sup>2</sup>}}{\eqn{^2}}, net benefit, and standardised net benefit.

The calibration plot and decision curve are drawn directly to the active
graphics device using base-R graphics and cannot be stored as objects.
The distribution plot is a ggplot2 object and can be stored and re-displayed.

This function currently supports binary outcomes only (0/1). Multinomial
support is planned for a future release.
}
\examples{
\dontrun{
result <- secure_model_validation(
  repo_owner      = "developer",
  repo_name       = "models",
  model_id        = "model_001",
  github_token    = Sys.getenv("GITHUB_PAT"),
  validation_data = my_data,
  outcome         = "event"
)

perf <- eval_performance(
  result,
  n_boot             = 200,
  decision_threshold = 0.1
)

# View metrics table
perf$metrics

# Recall the distribution plot (ggplot2 object)
perf$plots$distribution

# Calibration and decision curve plots were already drawn
# to the graphics device during the function call and
# cannot be recalled — re-run the function to redisplay them
}
}
\seealso{
\code{\link{secure_model_validation}}
}
